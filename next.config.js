/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.google.com','images.unsplash.com'], // Allow images from any domain
      },
}

module.exports = nextConfig
